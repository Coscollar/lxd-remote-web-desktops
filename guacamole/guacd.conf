# FASE 4 — guacd bind loopback (regla de oro: 4822 solo desde localhost).
# Sin este archivo, guacd arranca con default 0.0.0.0:4822 y se expone
# a todas las redes del host (lab-persistent, admin-net, etc.).
[server]
bind_host = 127.0.0.1
bind_port = 4822